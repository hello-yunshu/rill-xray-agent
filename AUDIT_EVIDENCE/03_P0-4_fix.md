# 03 P0-4 Fix (Rill side)

Captured 2026-08-06 after source/process gates passed.

## Change
- Branch `fix/rc1-audit-blockers`.
- RootTransaction: full crash-recoverable state machine; work dirs keyed by
  `digest(recommendationId)`; recommendationId validated
  (`^[A-Za-z0-9_-]{1,128}$`, rejected before any path use).
- Startup recovery restores the managed file from the prepare-time backup and
  materializes a `rolledBack` terminal bundle for any intermediate state, so the
  system always knows the effective configuration.

## Verification
- 34 unit tests pass (8 new root-txn tests).
- `scripts/run_all_checks.py`: source/process gates pass.

## Differential vs baseline finding P0-4
- Was: recovery only handled commit-bundle state; recommendationId used directly
  as a directory (path injection risk).
- Now: all crash states recoverable and auditable; ids hashed and validated.