# 01 P0-2 Fix (Rill side)

Captured 2026-08-06 after source/process gates passed.

## Change
- Branch `fix/rc1-audit-blockers`, commit `ed0236c`.
- State/audit promoted to a single recoverable transaction via an operation WAL
  (`python/rill_xray_agent/operation.py`) with two-stage audit append
  (`reserve`/`commit_event` in `audit.py`), startup recovery, and fault injection.
- State is never persisted when the audit cannot commit (capacity fails closed at reserve).

## Verification
- 20 unit tests pass (5 new atomicity tests covering all WAL fault points).
- `scripts/run_all_checks.py`: all source/process gates pass (seed=0).

## Differential vs baseline finding P0-2
- Was: state written, then audit appended (crash between the two left them divergent).
- Now: intent -> state -> audit -> terminal with crash recovery in either direction.