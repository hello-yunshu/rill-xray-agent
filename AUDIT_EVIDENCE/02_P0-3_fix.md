# 02 P0-3 Fix (Rill side)

Captured 2026-08-06 after source/process gates passed.

## Change
- Branch `fix/rc1-audit-blockers`, commit follows P0-2 evidence.
- Completed eviction now writes a permanent `closed` tombstone
  (`decisionIdHash`, `identityHash`, `payloadHash`, `closedAtEpochSeconds`) instead of deleting.
- Replay and conflict semantics preserved across eviction in both `RuntimeState` and the service path.

## Verification
- 26 unit tests pass (6 new closed-ledger tests).
- `scripts/run_all_checks.py`: all source/process gates pass.

## Differential vs baseline finding P0-3
- Was: eviction deleted the completed record, losing replay identity semantics.
- Now: evicted records live forever in `closed` as tombstones; idempotent replays and identity
  conflicts are still detected after eviction.