# 2026-08-09 R6 bootstrap-delivery qualification

Independent-audit-driven close-out round for the merge-candidate state of PR
#1 (rill-xray-agent, fix/rc1-audit-blockers) and PR #54 (Xray_bash_onekey,
feat/rill-xray-agent). Minimum-scope: no redesign of runtime/agent/WAL/CL,
no new features, no version bump, no re-run of expensive production gates.

## Audit finding addressed (P1)
R5 qualification subject covered the canonical bundle and the install
surfaces but NOT the actual Xray delivery path used by hosts:
`scripts/rill_xray_agent_bootstrap.sh` + `assets/rill-xray-agent-xray-bundle.tar.gz`.
The R5 five-mode qualification installed from the frozen bundle directly;
nothing proved "current Xray bootstrap -> current Xray asset -> install".

## Changes
- `scripts/build_qualification_subject.py`: deterministic generator/verifier
  (`--write` / `--check`) added. Documented tree scheme (sorted
  `"{relpath} {sha256}"`, sha256 over newline-joined lines). Scheme fidelity:
  systemdTreeSha256 reproduces the R5 sealed value exactly, install.sh /
  manifest / bundle reproduce their R5 values exactly. `--check` is non-zero
  on any drift; the frozen R5 `productionTreeSha256` is carried forward and
  freeze-checked (it was a one-off seal, not tool-recordable).
- `qualification/QUALIFICATION_SUBJECT.json`: regenerated by the tool —
  added `xray.bootstrapSha256` (09c1cf44…), `xray.bundleAssetSha256`
  (bff2455c…, == canonical bundle), `xray.deliveryTreeSha256` (9c3cc391…);
  executedAt.rillCommit moved to the canonical 638855e… (production identity,
  not docs HEAD), executedAt.xrayCommit = dac0c509…; payloadTreeSha256
  normalized (fead3604…) — payload BYTES unchanged; new subjectId
  d96ef7f950f4905004ca096210a505e88d6174119c19a7d56f24dab0c98697d2.
- `qualification/bootstrap-delivery.log`: raw targeted smoke evidence.
- `qualification/README.md`: split Production vs Delivery qualification,
  kept "Docker-only" scoping.
- Xray: `.github/test/test_rill_bootstrap_delivery.sh` (new; stage-installs
  via the real bundled installer; asserts SHA match, extraction, root
  members, artifacts, config invariants) wired into the required
  `test_rill_xray_agent.sh` suite. Durable uninstall tests untouched.
- PROJECT_MEMORY: current-state restructured around the production identity;
  blockers file re-titled; this record appended.

## Targeted delivery smoke (R6, fresh container, throwaway)
Subject: Xray branch HEAD `dac0c509dcfa8f6eb24b63de1e45f8855dd47b80`;
bootstrap sha256 `09c1cf441f57…`; asset sha256 `bff2455c3550…`;
EXPECTED_SHA256 == actual asset SHA verified at runtime; tar extraction and
root-member validation ran; real installer (bundle) exited 0 (staged
DESTDIR install); artifact set and default-config invariants
(mode=observe-only, routeAssistEnabled=false, boundedAutoAllowed=false,
localOnly=true) PASS. Image: `rill-pid1-debian12:r5`
sha256:29caa21f56a635d58f1a7d033842df7a5f65b64e7ee96ca45466fcd85c2a7010.
PID1 lifecycle intentionally NOT re-run (R5 covered it; no production byte
changed this round). Raw log: qualification/bootstrap-delivery.log.

## Gates NOT re-run and why
No production byte changed: Rill productionTreeSha256, bundleSha256,
canonical manifest, Xray install.sh, systemd tree, and the bundled payload
bytes are byte-identical to R5. Only fingerprints/subject bookkeeping plus
delivery surface evidence were added, so fresh 20/20, Debian/Ubuntu PID1
66/66, five-mode 34/34 x5 and the deterministic build were NOT re-executed.

## CI
Rill Source Gates: re-run pending after this push.
Xray "Rill Xray Agent" (pin 638855e…): re-check; "Test Install": re-check —
both now also carry the bootstrap-delivery regression.

## External prompt orphan
Commit `52d7632ddb420e0e2d3b894e17bf96240dae32e8` /
`00_总执行提示词.md` still public-fetchable (HTTP 200) — EXTERNAL P0, status
unchanged. No filter-repo re-run; merge remains blocked on this external
item per project policy.

## Verdict (code-level)
- CODE-LEVEL ZERO-P0: YES. CODE-LEVEL ZERO-P1: YES.
- DELIVERY QUALIFICATION: PASS (Docker scope).
- OVERALL zero-P0: FAIL until the historical prompt blob is inaccessible.
- READY: NO. MERGE: blocked by EXTERNAL PUBLIC-PROMPT P0. RC.2/Pre-release/
  Stable: blocked.