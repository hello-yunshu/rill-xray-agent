# Host integration and release candidate qualification

Applied the Xray host integration to `hello-yunshu/Xray_bash_onekey` branch `feat/rill-xray-agent` (based on upstream anchor e3ba5d7474498fbb556b0cae741a629ebb3bf1cd, no drift). Lifecycle coordination (reconfigure enter/leave, host-healthy guard, uninstall enter/finish, update_sh integration-schema gate) is wired into install.sh without altering host transaction return codes; mandatory host-integration contract checks run inside test-install.yml; i18n POTFILES.in includes the manager script. Source repo gates now pass inside a git checkout (repo-aware tree/sums) and the source-gates CI is green. Draft PR #54 open with all checks green (5 install variants, integration, security-regression). Randomized full-gate orders: 20/20 PASS (seeds 1..20). Gitleaks: no new findings (2 pre-existing upstream findings in test_phase1_safety.sh). Staged DESTDIR install/verify PASS; coordination suite 14/14 PASS. Status: rc candidate qualified at source/process/CI level; real PID1/systemd, supported-distribution and real Xray/Nginx/Fail2ban qualification remain open boundaries for later milestones. Version promoted to 0.1.0-rc.1; stable not allowed.
