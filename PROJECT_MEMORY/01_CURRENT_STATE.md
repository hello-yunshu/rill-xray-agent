# Current state

ALPHA AUDIT-REPAIR phase. Version stays 0.1.0-rc.1 for the audit-repair branch, but release qualification is BLOCKED: `preReleaseAllowed=false`, `sourceProcessQualified=false`, `stableAllowed=false`. Rill `main` Source Gates fail (run 31096443942); package manifest inconsistent; 7 P0 findings open (state/audit atomicity, replay tombstones, root transaction recovery, IPC peer credentials/bounded concurrency, script update guard, four-party mode transition, real-path observation). PR #54 remains Draft. Re-qualification restarts from round 1 with fresh seeds once all P0 close. `v0.1.0-rc.2` tag/Release forbidden until merged main CI is green and real PID1/systemd gates pass. Route Assist OFF, bounded auto OFF, observe-only default.
